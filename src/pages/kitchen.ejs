<%-
  include('../components/_header', {
    page: meta['kitchen']
  })
%>


<section class="section is-paddingless">
  <div class="container is-paddingless">

    <h1 class="is-hidden-mobile slide-caption title is-3">キッチンリフォーム</h1>
    <h1 class="is-hidden-tablet slide-caption title is-5">キッチンリフォーム</h1>
    <div class="columns">
      <div class="column">
        <img src="images/kitchen-fv.jpg" width="100%">
      </div>
    </div>

  </div>
</section>

<section class="section">
  <div class="container is-fluid">
    <h1 class="title is-3"><span class="underline-gray">商品ラインナップ</span></h1>
    <div class="columns is-multiline">
      <% for (key in kitchen['items']) { %>
      <% if(kitchen['items'][key].visible) { %>
      <div class="column is-3 has-text-centered is-flex">

        <div class="columns is-multiline">
          <div class="column is-12">
            <img src="<%= kitchen['items'][key].product_image %>"
              alt="<%= kitchen['items'][key].product_image_description %>" width="200" />
          </div>

          <div class="column is-12">
            <p class="has-text-left">
              <span class="has-text-weight-bold">
                <%= kitchen['items'][key].manufacturer + ' ' + kitchen['items'][key].series %>
              </span>
            </p>
            <p class="has-text-left">
              <%= kitchen['items'][key].description %>
            </p>
          </div>

          <div class="column is-12">
            <p class="is-size-6">
              <% if(kitchen['items'][key].retail_price != 0) { %>
              希望小売価格 ￥<%= kitchen['items'][key].retail_price.toLocaleString() %>円
              <% } %>
            </p>
            <p class="is-size-4">
              <span class="underline-red">
                ￥<%= kitchen['items'][key].price.toLocaleString() %>円
              </span>
            </p>
          </div>
        </div>
      </div>
      <hr>
      <% } %>
      <% } %>
    </div>

  </div>
</section>

<% include ../components/_footer %>